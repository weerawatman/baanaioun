/**
 * Re-export useAuth from AuthContext so it can be imported
 * from the shared hooks barrel (@/shared/hooks) like all other hooks.
 *
 * @example
 * import { useAuth } from '@/shared/hooks';
 */
export { useAuth } from '@/contexts/AuthContext';
